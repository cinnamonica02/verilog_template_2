[project]
name = "open-tpu-tinytapeout"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "cocotb==2.0.1",
  "pytest==8.4.2",
]

[project.optional-dependencies]
jax = ["jax"]

[tool.uv]
package = false
